export default function Skeleton({ className = '' }) {
  return (
    <div className={`animate-pulse bg-ink/10 rounded-xl ${className}`} />
  )
}
/**
 * Componente Skeleton.
 * Muestra placeholders mientras carga contenido.
 */
